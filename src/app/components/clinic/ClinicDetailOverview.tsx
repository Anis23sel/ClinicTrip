import type { ClinicDetailData } from "./ClinicDetailTypes";

export default function ClinicDetailOverview({ clinic }: { clinic: ClinicDetailData }) {
  return <div className="space-y-6"><section><h3 className="mb-3 text-xl font-semibold">Clinic Information</h3><div className="space-y-1 text-muted-foreground"><p>{clinic.address}</p>{clinic.phone && <p>{clinic.phone}</p>}{clinic.website && <a href={clinic.website} target="_blank" rel="noreferrer" className="text-primary hover:underline">Visit clinic website</a>}</div></section><section><h3 className="mb-3 text-xl font-semibold">Available Procedures</h3>{clinic.procedures.length === 0 ? <p className="text-muted-foreground">No procedures are available for this clinic yet.</p> : <div className="space-y-4">{clinic.procedures.map((procedure) => <article key={procedure.id} className="rounded-xl border border-border p-4"><h4 className="mb-1 font-semibold">{procedure.name}</h4>{procedure.category && <p className="text-sm text-primary">{procedure.category}</p>}{procedure.description && <p className="my-3 text-sm text-muted-foreground">{procedure.description}</p>}<div className="grid grid-cols-2 gap-4"><div><p className="text-xs text-muted-foreground">Duration</p><p className="text-sm font-medium">{procedure.duration || "Not specified"}</p></div><div><p className="text-xs text-muted-foreground">Gender scope</p><p className="text-sm font-medium">{procedure.genderScope || "All patients"}</p></div></div><div className="mt-3 text-sm text-muted-foreground">Starting price</div><div className="text-2xl font-bold text-primary">${procedure.startingPrice.toLocaleString()}</div></article>)}</div>}</section></div>;
}
