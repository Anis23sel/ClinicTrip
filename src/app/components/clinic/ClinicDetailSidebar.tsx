import { MapPin } from "lucide-react";
import type { ClinicDetailData } from "./ClinicDetailTypes";

export default function ClinicDetailSidebar({ clinic, onContact }: { clinic: ClinicDetailData; onContact: () => void }) { return <aside className="space-y-5"><div className="rounded-xl border-2 border-primary bg-primary/10 p-6"><button onClick={onContact} className="mt-5 inline-block w-full rounded-lg bg-primary px-6 py-3 text-center font-semibold text-primary-foreground transition-opacity hover:opacity-90">Contact the clinic and start now</button></div><div className="rounded-xl border border-border bg-card p-5"><h3 className="mb-3 font-semibold">Route Map</h3><div className="flex aspect-square items-center justify-center rounded-lg bg-gradient-to-br from-primary/20 to-primary/10"><div className="text-center text-muted-foreground"><MapPin size={44} className="mx-auto mb-2 text-primary/50" /><p className="text-sm font-medium">Hotel ↔ Clinic</p><p className="mt-1 text-xs opacity-70">{clinic.city}, {clinic.country}</p></div></div></div></aside>; }