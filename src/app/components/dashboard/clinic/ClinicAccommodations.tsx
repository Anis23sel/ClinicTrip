import { Edit, Plus, Trash2 } from 'lucide-react';
import { DashboardList } from './ClinicDoctors';

const accommodations = [{ id: 1, name: 'Partner Hotel Premium', distance: '5 min walk from clinic', roomTypes: [{ type: 'Single Room', price: 80 }, { type: 'Double Room', price: 100 }, { type: 'Suite', price: 150 }] }, { id: 2, name: 'Partner Hotel Standard', distance: '10 min walk from clinic', roomTypes: [{ type: 'Single Room', price: 50 }, { type: 'Double Room', price: 70 }] }];

export default function ClinicAccommodations() { return <DashboardList title="Accommodation Partners" action="Add Accommodation"><div className="space-y-4">{accommodations.map((accommodation) => <div key={accommodation.id} className="rounded-lg border border-border bg-card p-6"><div className="mb-4 flex items-start justify-between"><div><h3 className="mb-1 text-xl font-semibold">{accommodation.name}</h3><p className="text-sm text-muted-foreground">{accommodation.distance}</p></div><div className="flex gap-2"><button aria-label="Edit accommodation" className="rounded-lg p-2 hover:bg-accent"><Edit size={18} /></button><button aria-label="Delete accommodation" className="rounded-lg p-2 text-destructive hover:bg-destructive/10"><Trash2 size={18} /></button></div></div><h4 className="mb-2 text-sm font-semibold">Room Types</h4><div className="grid gap-3 md:grid-cols-2">{accommodation.roomTypes.map((room) => <div key={room.type} className="flex items-center justify-between rounded bg-accent/30 p-3"><span>{room.type}</span><span className="font-semibold text-primary">${room.price}/night</span></div>)}</div></div>)}</div></DashboardList>; }