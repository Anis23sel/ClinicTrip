import { CheckCircle, Clock } from 'lucide-react';

const bookings = [{ id: 1, patientName: 'Sarah Martinez', procedure: 'Rhinoplasty', doctor: 'Dr. Mehmet Yilmaz', date: 'May 15, 2026', price: 2500, status: 'pending' as const }, { id: 2, patientName: 'John Smith', procedure: 'Hair Transplant', doctor: 'Dr. Ayse Demir', date: 'May 20, 2026', price: 1800, status: 'confirmed' as const }, { id: 3, patientName: 'Emma Johnson', procedure: 'Breast Augmentation', doctor: 'Dr. Mehmet Yilmaz', date: 'April 5, 2026', price: 3200, status: 'completed' as const }];

export default function ClinicBookings() { return <section><h2 className="mb-6 text-2xl font-bold">Booking Management</h2><div className="space-y-4">{bookings.map((booking) => <div key={booking.id} className="rounded-lg border border-border bg-card p-6"><div className="mb-4 flex items-start justify-between"><div><h3 className="mb-1 text-xl font-semibold">{booking.patientName}</h3><p className="text-muted-foreground">{booking.procedure}</p></div><Status status={booking.status} /></div><div className="mb-4 grid gap-4 md:grid-cols-3"><Metric label="Date" value={booking.date} /><Metric label="Doctor" value={booking.doctor} /><Metric label="Price" value={`$${booking.price.toLocaleString()}`} /></div>{booking.status === 'pending' && <div className="flex gap-3"><button className="rounded-lg bg-primary px-4 py-2 text-primary-foreground">Accept</button><button className="rounded-lg border border-destructive px-4 py-2 text-destructive">Reject</button></div>}{booking.status === 'confirmed' && <button className="rounded-lg bg-blue-600 px-4 py-2 text-white">Mark as Completed</button>}</div>)}</div></section>; }
function Status({ status }: { status: string }) { const Icon = status === 'pending' ? Clock : CheckCircle; return <div className={`flex items-center gap-2 rounded-full px-3 py-1 text-sm ${status === 'confirmed' ? 'bg-green-100 text-green-800' : status === 'pending' ? 'bg-yellow-100 text-yellow-800' : 'bg-blue-100 text-blue-800'}`}><Icon size={16} /><span className="capitalize">{status}</span></div>; }
function Metric({ label, value }: { label: string; value: string }) { return <div><p className="text-sm text-muted-foreground">{label}</p><p className="font-medium">{value}</p></div>; }